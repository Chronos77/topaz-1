version: '3.1'

services:
  # The DB service
  db:
    image: mariadb
    restart: always
    environment:
      MYSQL_USER: FlemeAdmin
      MYSQL_PASSWORD: Fl3m3
      MYSQL_ROOT_PASSWORD: Tr0pL@Fl3m3
      MYSQL_DATABASE: tpz-fleme
    # Run all the .sql files in the /sql directory to initalize the DB. This only hapens the first time this service is started and will not handle additions/modifications
    volumes:
      - ./sql:/docker-entrypoint-initdb.d
      - /server-data/:/var/lib/mysql
    ports:
      - "3306:3306"

  # Ease of access tool for the DB, you can type in localhost:8080 to get a web interface to the DB. You can log in with root:topazisawesome
  db-admin-portal:
    image: adminer
    restart: always
    depends_on: 
      - "db"
    ports:
      - 8080:8080

  # The server service
  game:
    # Build whatever is in the Dockerfile in the topaz root folder
    build: .
    depends_on:
      - "db"
    ports:
      # topaz_connect
      - "54230:54230/tcp"
      - "54231:54231/tcp"
      - "54001:54001/tcp"
      # topaz_search
      - "54002:54002/tcp"
      # topaz_game
      - "54230:54230/udp"
